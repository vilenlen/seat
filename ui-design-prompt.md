# 任务：为「圆桌会 · Seat 一席」重新设计界面

你是一名资深的产品界面/交互设计师。请先完整理解下面这份功能描述，然后为这款产品设计一套**完整、现代、可用**的界面方案。你的产出重点是 UI/UX 设计（信息架构、页面/视图划分、组件、状态呈现、交互流程），而不是现有代码的结构。

请勿先动手写代码或改现有文件——先产出设计方案，并明确指出你对现有界面的改进点。

---

## 一、产品是什么

「圆桌会」（Seat · 一席）是一款 **多角色 AI 会议模拟** 工具：

- 用户（真人）扮演会议主持人/产品负责人，是固定参会者「你」。
- 用户创建一场会议（标题、议题、背景），从「角色库」里挑选若干 AI 角色（如产品经理、技术负责人、设计师、市场负责人）参会。
- 会议开始后，所有 AI 角色会自动循环推进讨论：**思考 → 举手抢发言权 → 一人发言 → 循环**，真人可随时插话参与。
- 核心价值：让用户和一群有不同立场、性格、职责的 AI「同事」就一个议题开会，用于推演、预演、头脑风暴。

---

## 二、核心会议循环（状态机，界面设计的灵魂）

一场会议的推进是一个循环，每个角色在任意时刻都处于以下状态之一：

1. **idle（空闲/聆听）** —— 默认状态
2. **thinking（思考）** —— 所有 AI 角色**并行**思考：判断「我有没有话要说」，有话要说就准备好发言内容并「举手」
3. **competing（抢锁/竞争）** —— 举手想发言的角色进入竞争态，随机一人赢得「发言权」（lock）
4. **speaking（发言）** —— 得锁者打字机式逐字输出发言，其余角色停下聆听
5. **stopped（停止）** —— 有人发言时，其余角色为 stopped

顶层阶段状态 phase：`idle | thinking | competing | speaking`。
会议状态 status：`setup | running | paused | ended`。

界面需要能清晰呈现：当前处于哪个阶段、谁在思考、谁举手了、谁拿到了发言权、谁在发言。现有实现用「阶段指示条 + 角色名册状态徽章 + 发言锁芯片」三处协同表达，你可以重新设计这套表达。

**抢锁规则**：每个 AI 角色思考后返回一个 JSON 决定 `{"want": true/false, "say": "..."}`。want=true 的角色举手；有多个举手者时**随机**选一人发言；无人举手则该轮结束，系统提示等待真人发言。

**真人（你）**：始终固定参会，随时可输入发言；发言后触发所有 AI 角色进入新一轮思考。

---

## 三、角色系统

**角色（Role）字段**：
- `name` 姓名（如「李想」）
- `duty` 职责/岗位（如「产品经理」）
- `personality` 性格（如「直接、务实、爱拍板」）
- `style` 说话风格（如「观点鲜明、爱举例子」）
- `background` 背景（经历、立场、目标）
- `group` 所属分组（角色库的分组管理）
- `color` 头像颜色（自动生成、互不撞色的色相）
- `provider` 绑定的推理底座（可选，见下）

**角色库**：可持久化的角色集合，支持分组（增删改、重命名）。角色支持新建/编辑/删除，头像颜色自动配色可换。

**「你」**：内置固定角色，不可删除，职责默认「产品负责人 · 主持」，始终参会。

---

## 四、主要界面/视图

当前是单页应用，包含以下视图与弹窗（你需评估是否保留这种结构，或重新组织）：

### 视图 1：会议设置页（setup）
- 顶部导航：账户/登录按钮、返回会议按钮
- **会议信息**：会议标题、议题、背景说明（三个输入框）
- **与会角色**：从角色库勾选本次参会角色（卡片式多选），入口按钮「管理角色库」
- **会议参数**：开关「深度思考」（开启后 AI 思考更充分、耗时更长）
- **推理引擎**：显示当前引擎摘要 + 入口「配置推理引擎」
- 主按钮「开始会议」

### 视图 2：会议进行页（room）—— 产品核心界面
- 顶部栏：会议标题/议题、状态胶囊（进行中/已暂停/已结束）、阶段指示条、发言锁芯片、引擎设置、深度思考迷你开关、暂停/结束按钮
- 侧边「与会角色」名册：列出所有角色及其**实时状态徽章**（思考中的动画点、举手、发言、停止等）
- 中央聊天区：按时间顺序的消息流，每条消息带角色名/头像/时间；AI 发言为打字机逐字上屏效果；系统消息（如「某某获得发言权」「会议结束」）居中呈现
- 底部输入框：「作为『你』发言」，回车发送、Shift+回车换行

### 视图 3：角色库管理页（library）
- 左侧分组栏（全部/未分组/各分组，含新建、重命名、删除分组）
- 右侧角色网格：角色卡片 + 新增按钮
- 点击角色打开「编辑角色」弹窗

### 弹窗/抽屉
- **编辑角色弹窗**：姓名、职责、分组下拉、推理底座下拉、性格、说话风格、背景、头像颜色（可换色）
- **角色档案抽屉**：从会议名册点击角色，只读查看其完整档案
- **推理引擎设置弹窗**：见下
- **账户与订阅弹窗**：见下

---

## 五、推理引擎（关键的多模式配置）

会议中的 AI 发言由「推理引擎」驱动，共 4 种模式，用户可在设置弹窗中选择：

1. **演示模式（demo）**：内置脚本生成发言，无需任何配置，开箱即用。
2. **本地 Agent（local）**：调用用户本机已登录的 claude / codex CLI（经本地桥接服务 `bridge.mjs`），推理在本机完成。配置项：Agent 选择（claude/codex）、桥接地址、模型名，附「检测桥接连接」健康检查。
3. **云端 LLM（cloud）**：调用 OpenAI 兼容或 Anthropic 接口。配置项：底座类型（OpenAI/Anthropic）、Base URL、API Key、模型名。
4. **托管 AI（hosted）**：由产品方代理调用 OpenAI，免填 Key，但需要登录；有免费额度和订阅。

**多底座（Provider）机制**：除「默认底座」外，用户可新建多个命名「底座」，每个底座各自保存一种模式配置；**每个 AI 角色可单独绑定某个底座**（实现不同角色用不同模型）。引擎设置弹窗需支持：选择/新建/删除/重命名底座，及按底座编辑四种模式的参数。

---

## 六、托管模式：登录 / 订阅 / 额度

托管模式配套一套账号与计费体系：

- **登录**：邮箱 + 魔法链接（无密码）登录；右上角显示「登录」或已登录邮箱。
- **套餐**：免费试用（一次性 20k tokens）与 Pro（每月 200 万 tokens，按自然月重置）。
- **额度展示**：进度条展示 `已用 / 总额度 tokens`，如「12k / 2000k tokens」。
- **升级 Pro**：跳转第三方支付（Lemon Squeezy）完成订阅；支付回调后额度自动刷新（轮询，最多 30 秒）。
- 额度耗尽时，托管推理返回 402，界面需提示「额度已用完，请升级到 Pro」并自动刷新额度条。

登录/额度信息在两处出现：独立的「账户与订阅」弹窗，以及「推理引擎设置」弹窗内的托管模式面板。

---

## 七、技术约束与背景（供设计参考，非必须复刻）

- 当前前端是**单个 HTML 文件**（约 2200 行，含内联 CSS/JS），后端为 Vercel Serverless 函数 + Supabase（登录/数据库）+ Lemon Squeezy（支付）+ OpenAI。
- 数据本地持久化用 `localStorage`（角色库、勾选、引擎配置、底座列表）；账号/额度在服务端。
- 后端接口：`/api/chat`（托管推理+记账）、`/api/billing`（套餐额度查询）、`/api/subscribe`（创建结账）、`/api/config`（公开配置）、`/api/webhook/lemonsqueezy`（订阅事件）。
- 设计时不必拘泥于「单文件」这一实现细节——你应输出一套理想的信息架构与组件设计。

---

## 八、你的产出要求

请输出一份完整的界面设计方案，至少包含：

1. **信息架构**：页面/视图如何组织，导航如何设计（保留单页还是拆分）。
2. **每个视图的布局说明**：可用 ASCII 线框图或组件清单描述，重点说明「会议进行页」的布局。
3. **会议循环的状态可视化**：如何让用户一眼看懂「谁在思考 / 谁举手 / 谁在发言 / 谁在听」，给出你的设计方案。
4. **关键组件**：角色卡片、名册状态徽章、消息气泡、打字机效果、额度进度条、引擎设置面板等的设计要点。
5. **对现有界面的改进建议**：指出当前实现中你认为体验不佳或可优化的地方，并给出改进方案。
6. **视觉基调建议**：配色、字体、质感（现有界面为浅色现代风，使用 oklch 色彩、圆角卡片），说明你倾向的方向。

请用中文输出，结构清晰、可直接指导后续实现。
